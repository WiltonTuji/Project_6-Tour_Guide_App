<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.project6_wiltontuji">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RetaurantesActivity"
            android:label="Restaurantes"/>
        <activity android:name=".MuseusActivity"
            android:label="Museus"/>
        <activity android:name=".VidaNoturnaActivity"
            android:label="Vida Noturna"/>
        <activity android:name=".HoteisActivity"
            android:label="Hoteis"></activity>
    </application>

</manifest>